#### Launching App Using Nodejs

Install dependencies from the root directory.

```sh
$ npm install
```

Start the app from the root directory.

```sh
$ npm start
```

Open your browser, go to [http://localhost:8080/](http://localhost:8080/)